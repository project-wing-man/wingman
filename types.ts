export type flightCardProps = {
    airline: string,
    adults: number,
    departureTime: Date,
    arrivalTime: Date,
    departAirport: string,
    arrivalAirport: string, 
    price: string,
    nonStop: boolean
}